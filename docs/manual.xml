<?xml version='1.0'?>
<!DOCTYPE book PUBLIC "-//Norman Walsh//DTD DocBk XML V4.2//EN"
                    "http://www.oasis-open.org/docbook/xml/4.2/docbookx.dtd">

<!--
  -
  -  file:  manual.xml
  -
  -  Copyright (c) 2003-2012 Michael E. Smoot .
  -  Copyright (c) 2012-2016 Daniel Aarno.
  -  Copyright (c) 2017-2026 Google LLC.
  -  All rights reserved.
  -
  -  See the file COPYING in the top directory of this distribution for
  -  more information.
  -
  -  THE SOFTWARE IS PROVIDED _AS IS_, WITHOUT WARRANTY OF ANY KIND, EXPRESS
  -  OR IMPLIED, INCLUDING BUT NOT LIMITED TO THE WARRANTIES OF MERCHANTABILITY,
  -  FITNESS FOR A PARTICULAR PURPOSE AND NON-INFRINGEMENT. IN NO EVENT SHALL
  -  THE AUTHORS OR COPYRIGHT HOLDERS BE LIABLE FOR ANY CLAIM, DAMAGES OR OTHER
  -  LIABILITY, WHETHER IN AN ACTION OF CONTRACT, TORT OR OTHERWISE, ARISING
  -  FROM, OUT OF OR IN CONNECTION WITH THE SOFTWARE OR THE USE OR OTHER
  -  DEALINGS IN THE SOFTWARE.
  -
  -->
<book>
<bookinfo>
<title>Templatized C++ Command Line Parser Manual (v2.0)</title>
    <author>
	<firstname>Daniel</firstname>
	<surname>Aarno</surname>
    </author>

    <author>
	<firstname>Michael</firstname>
	<surname>Smoot</surname>
	<othername role='mi'>E</othername>
    </author>

    <copyright>
	<year>2017-2026</year>
	<holder>Google LLC</holder>
    </copyright>

    <copyright>
	<year>2012-2016</year>
	<holder>Daniel Aarno</holder>
    </copyright>

    <copyright>
	<year>2003-2012</year>
	<holder>Michael E. Smoot</holder>
    </copyright>
</bookinfo>

<chapter id="BASIC_USAGE">
<title>Basic Usage</title>
<sect1 id="OVERVIEW">
<title>Overview</title>
<para>
This manual describes <emphasis>TCLAP 2.0</emphasis>, which is not
source compatible with the 1.4/1.6 branches: every argument is now
constructed with a single named-field aggregate instead of a list of
positional constructor parameters, and an argument is never
self-registered with a <classname>CmdLine</classname> from inside its
own constructor. If you're maintaining code written against 1.4 or
1.6, keep an eye out for the notes throughout this manual that call
out where 2.0 differs, or use the 1.4 (C++98) or 1.6 (same API,
modernized internals) branches instead if you don't want these
changes.
</para>
<para>
<emphasis>TCLAP</emphasis> has a few key classes to be aware of.
The first is the
<classname>CmdLine</classname> (command line) class. This class parses
the command line passed to it according to the arguments that it
contains. Arguments are separate objects that are added to the
<classname>CmdLine</classname> object one at a time. The six
argument classes are: <classname>ValueArg</classname>,
<classname>UnlabeledValueArg</classname>,
<classname>SwitchArg</classname>, <classname>MultiSwitchArg</classname>,
<classname>MultiArg</classname> and
<classname>UnlabeledMultiArg</classname>.
These classes are templatized, which means they can be defined to parse
a value of any <link linkend="USING_ARGTRAITS"> type</link>. Once you add the
arguments to the <classname>CmdLine</classname> object, it parses the
command line
and assigns the data it finds to the specific argument objects it
contains. Your program accesses the values parsed by
calls to the <methodname>value()</methodname> methods of the
argument objects.
</para>
</sect1>

<sect1 id="EXAMPLE">
<title>Example</title>
<para>
Here is a simple <ulink url="test1.cpp"> example</ulink> ... Each
argument is constructed by passing its constructor a single aggregate
of named fields -- a C++20 <emphasis>designated initializer</emphasis>,
e.g. <code>{.flag = "n", .name = "name", ...}</code> -- rather than the
long, order-sensitive list of positional parameters used before 2.0.
Every argument must then be added to the <classname>CmdLine</classname>
with an explicit call to <methodname>add()</methodname>: 2.0 removed
the constructor overloads that added an argument to a
<classname>CmdLine</classname> passed in as a constructor parameter
(see <link linkend="ARG_ADD_CMDLINE">this note</link> if you're
migrating code that relied on that).

<programlisting>
#include &lt;string&gt;
#include &lt;iostream&gt;
#include &lt;algorithm&gt;
#include &lt;tclap/CmdLine.h&gt;

int main(int argc, char** argv)
{

	// Define the command line object, and insert a message
	// that describes the program. The "Command description message"
	// is printed last in the help text.  The CmdLine object parses
	// the argv array based on the Arg objects that it contains.
	TCLAP::CmdLine cmd({.message = "Command description message",
	                    .version = "0.9"});

	// Define a value argument and add it to the command line.
	// A value arg defines a flag and a type of value that it expects,
	// such as "-n Bishop".
	TCLAP::ValueArg&lt;std::string&gt; nameArg({.flag = "n",
	                                      .name = "name",
	                                      .description = "Name to print",
	                                      .required = true,
	                                      .defaultValue = "homer"});

	// Add the argument nameArg to the CmdLine object. The CmdLine object
	// uses this Arg to parse the command line.
	cmd.add( nameArg );

	// Define a switch and add it to the command line.
	// A switch arg is a boolean argument and only defines a flag that
	// indicates true or false.
	TCLAP::SwitchArg reverseSwitch({.flag = "r",
	                                .name = "reverse",
	                                .description = "Print name backwards"});
	cmd.add( reverseSwitch );

	// Parse the argv array. parseOrExit() prints an error/usage/version
	// message and calls exit() for you on a malformed command line or
	// --help/--version, the same way every Arg exception used to get
	// turned into an exit() automatically before 2.0 -- there is no
	// longer a try/catch block to write for that case.
	cmd.parseOrExit(argc, argv);

	// Get the value parsed by each arg.
	std::string name = nameArg.value();
	bool reverseName = reverseSwitch.value();

	// Do what you intend.
	if ( reverseName )
	{
		std::reverse(name.begin(),name.end());
		std::cout &lt;&lt; "My name (spelled backwards) is: " &lt;&lt; name &lt;&lt; std::endl;
	}
	else
		std::cout &lt;&lt; "My name is: " &lt;&lt; name &lt;&lt; std::endl;
}
</programlisting>

<warning id="SPEC_EXCEPTION_WARNING">
<para>
This example never wraps anything in a try block, and that's
deliberate: as of 2.0, a malformed command line no longer throws (see
<methodname>parseOrExit()</methodname>'s comment above), and every
<classname>Arg</classname> here is constructed with a fixed,
hardcoded spec, so the one exception that
<emphasis>could</emphasis> still be thrown --
<classname>SpecificationException</classname>, for a duplicate
flag/name or a blank <code>.name</code> -- can only happen here as a
result of a bug in this file, not from anything a user types. Left
uncaught, it crashes the program with an unhandled-exception message
(which does include <methodname>what()</methodname>'s
<quote>argId -- errorText</quote> text, just not a nicely formatted
one) -- which is the point: it surfaces loudly during development,
the same way you'd want any other precondition violation to.
</para>
<para>
That reasoning stops applying the moment your <classname>Arg</classname>
specs aren't all fixed at compile time -- e.g. flags built from a
config file, a plugin registering its own options, or any other
runtime-determined spec. There, a duplicate name is something your
own input data can trigger, not just a coding mistake, and it deserves
the same treatment as a parse error: catch
<classname>SpecificationException</classname> yourself around the
<classname>CmdLine</classname>/<classname>Arg</classname> construction
code and report it the way you'd report bad input, rather than letting
it crash the whole program.
</para>
</warning>

The output should look like:

<programlisting>

% test1 -n mike
My name is: mike

% test1 -n mike -r
My name (spelled backwards) is: ekim

% test1 -r -n mike
My name (spelled backwards) is: ekim

% test1 -r
PARSE ERROR:
             Required argument missing: name

Brief USAGE:
   test1  [-hr] [--version] -n &lt;string&gt;

For complete USAGE and HELP type:
   test1 --help


% test1 --help

USAGE:

   test1  [-hr] [--version] -n &lt;string&gt;


Where:

   -n &lt;string&gt;,  --name &lt;string&gt;
     (required) Name to print

   -r,  --reverse
     Print name backwards

   --,  --ignore_rest
     Ignores the rest of the labeled arguments following this flag.

   --version
     Displays version information and exits.

   -h,  --help
     Displays usage information and exits.

   Command description message

</programlisting>
</para>

<sect2 id="LIBRARY_PROPERTIES">
<title>Library Properties</title>
<para>
This example shows a number of different properties of the
library...
<itemizedlist>
  <listitem>
    Arguments can appear in any order (...mostly, <link linkend="COMPLICATIONS"> more</link> on this later).
</listitem>

<listitem>The <parameter>help</parameter>, <parameter>version</parameter>
and <parameter>--</parameter> <classname>SwitchArg</classname>s
are specified automatically. Using either the <parameter>-h</parameter> or
<parameter>--help</parameter> flag will cause the USAGE message to be displayed,
<parameter>--version</parameter> will cause any version information to
be displayed (unlike the other automatic switches, it has no short flag
by default, so it doesn't collide with a real <parameter>-v</parameter>
your program might want), and <parameter>--</parameter> or
<parameter>--ignore_rest</parameter> will cause the
remaining labeled arguments to be ignored. These switches are
included by default on every command line. You can <link linkend="NO_HELP_VERSION">disable this functionality</link> if desired (although we don't recommend it).
</listitem>

<listitem>If there is an error parsing the command line (e.g. a required
argument isn't provided), the program exits and displays a brief
USAGE and an error message.</listitem>

<listitem>The program name is assumed to always be argv[0], so it isn't
specified directly.</listitem>

<listitem>A value delimiter character can be specified via the
<parameter>Dialect</parameter> passed to <classname>CmdLine</classname>
(see <link linkend="CHANGING_STARTSTRINGS">below</link>). This means
that if you prefer arguments of the style
<parameter>-s=asdf</parameter> instead of
<parameter>-s asdf</parameter>, you can do so.</listitem>

<listitem>A malformed command line (a missing required argument, an
unrecognized flag, a value that fails its type conversion or
constraint) no longer throws in 2.0 -- see the note about
<methodname>parseOrExit()</methodname> above. What still throws is
constructing the <classname>CmdLine</classname> or an
<classname>Arg</classname> with a malformed spec (a duplicate
flag/name, a blank <code>.name</code>, and the like): that always
throws a <classname>SpecificationException</classname>, uncaught, and
is never turned into a <classname>ParseOutcome</classname>. When every
<classname>Arg</classname> is constructed with a fixed, hardcoded spec
-- as in every example in this manual -- that exception can only fire
as a result of a bug in your code, so letting it propagate and crash
the program during development is normally the right thing to do, the
same way you'd want any other precondition violation to surface. If
your specs are instead built from something runtime-determined (a
config file, a plugin registering its own options), that assumption
stops holding and you should catch
<classname>SpecificationException</classname> yourself -- see the
warning in the <link linkend="EXAMPLE">Example</link>
section.</listitem>

</itemizedlist>
</para>
</sect2>

<sect2 id="ARG_PROPERTIES">
<title>Common Argument Properties</title>
<para>
Arguments, whatever their type, have a few common properties.
These properties are set as named fields (<code>.field = value</code>)
in the single designated-initializer aggregate passed to each
argument's constructor -- <classname>ValueArgSpec&lt;T&gt;</classname>
for <classname>ValueArg&lt;T&gt;</classname>,
<classname>SwitchArgSpec</classname> for
<classname>SwitchArg</classname>, and so on for every argument type.
You can write the spec type name out explicitly
(<code>ValueArg&lt;T&gt;(ValueArgSpec&lt;T&gt;{...})</code>), but it's
almost always safe to just write <code>ValueArg&lt;T&gt;({...})</code>
instead -- the braced list is enough for the compiler to pick the
right constructor, so naming the spec type again is redundant. See
<filename>test1.cpp</filename>, above, for an example.
<itemizedlist>
<listitem>First is the flag (<code>.flag</code>), the character preceded by a dash(-) that
signals the beginning of the argument on the command line.</listitem>

<listitem>Arguments also have a name (<code>.name</code>), which can also be used
as an alternative flag on the command line, this time preceded by two dashes
(--) [like the familiar <function>getopt_long()</function>]. Unlike every
other field, <code>.name</code> is <emphasis>mandatory</emphasis>: leaving
it out of the <code>{...}</code> list is a compile error rather than
silently producing a blank name.</listitem>

<listitem>Next is the description (<code>.description</code>) of the argument. This is a short
description of the argument displayed in the help/usage message
when needed.</listitem>

<listitem>The remaining fields vary depending on
the type of argument. Some possible ones include:
<itemizedlist>
<listitem>A boolean value (<code>.required</code>) indicating whether the Arg is required or not.  </listitem>
<listitem>A default value (<code>.defaultValue</code>).</listitem>
<listitem>A description of the type of value expected (<code>.typeDesc</code>).</listitem>
<listitem>A <link linkend="CONSTRAINT">constraint</link> on the value expected (<code>.constraint</code>).</listitem>
</itemizedlist>
</listitem>
<listitem>No field takes the <classname>CmdLine</classname> to register
the Arg with -- 2.0 removed the self-registering constructors that
existed for this in earlier versions. Always call
<methodname>cmd.add(arg)</methodname> explicitly after constructing the
Arg; see <link linkend="ARG_ADD_CMDLINE">below</link> if you're
migrating code that relied on the old behavior.</listitem>
<listitem>See the <ulink url="html/index.html">API Documentation</ulink> for more detail.</listitem>
</itemizedlist>
</para>
</sect2>
</sect1>

<sect1 id="COMPILING">
<title>Compiling</title>
<para>
<emphasis>TCLAP</emphasis> is implemented entirely in header files
which means you only need to include CmdLine.h to use the library.
<programlisting>
        #include &lt;tclap/CmdLine.h&gt;
</programlisting>
You'll need to make sure that your compiler can see the header
files. If you do the usual "make install" then your compiler should
see the files by default. Alternatively, you can use the -I
complier argument to specify the exact location of the libraries.
<programlisting>
        c++ -o my_program -I /some/place/tclap-2.X/include my_program.cpp
</programlisting>
Where /some/place/tclap-2.X is the place you have unpacked the
distribution.
</para>

<para>
Finally, if you want to include <emphasis>TCLAP</emphasis> as part of
your software
(which is perfectly OK, even encouraged) then simply copy the
contents of /some/place/tclap-2.X/include (the tclap directory and
all of the header files it contains) into your include
directory.
</para>

<para>
<emphasis>TCLAP 2.0</emphasis> requires a C++20 compiler --
it makes deliberate use of C++20 features (designated initializers,
concepts, and <classname>std::format</classname> where the standard
library provides it) throughout its public API and implementation.
This is unlike the <emphasis>1.4</emphasis> branch, which stays on
C++98 for maximum portability, and unlike <emphasis>1.6</emphasis>,
which is also a C++20 rewrite of TCLAP's internals but keeps 1.4's
original, source-compatible API rather than the one this manual
describes. If your project needs a C++98-compatible TCLAP, or you
want 2.0's modernized internals without the API changes this manual
describes, use the 1.4 or 1.6 branch instead. Please let us know if
your C++20 compiler doesn't work with 2.0!
</para>
</sect1>
</chapter>
<chapter id="FUNDAMENTAL_CLASSES">
<title>Fundamental Classes</title>
<sect1 id="COMMAND_LINE">
<title><classname>CmdLine</classname></title>
<para>
The <classname>CmdLine</classname> class contains the arguments that define
the command line and manages the parsing of the command line. The
<classname>CmdLine</classname> doesn't parse the command line itself it only
manages the parsing. The actual parsing of individual arguments occurs within
the arguments themselves. The <classname>CmdLine</classname> keeps track of
of the required arguments, <link linkend="ARG_GROUP">relationships</link>
between arguments, and <link linkend="CHANGE_OUTPUT">output</link> generation.
</para>
</sect1>
<sect1 id="SWITCH_ARG">
<title><classname>SwitchArg</classname></title>
<para><classname>SwitchArg</classname>s are what the name implies:
simple, on/off, boolean switches. Use <classname>SwitchArg</classname>s
anytime you want to turn
some sort of system property on or off. <classname>SwitchArg</classname>s
don't parse a value. They return <constant>TRUE</constant> or
<constant>FALSE</constant>, depending on whether the switch has been found
on the command line and what the default value was defined as.</para>
</sect1>

<sect1 id="VALUE_ARG">
<title><classname>ValueArg</classname></title>
<para><classname>ValueArg</classname>s are arguments that read a
value of some type
from the command line. Any time you need a file name, a number,
etc. use a <classname>ValueArg</classname> or one of its variants.
All <classname>ValueArg</classname>s are
<link linkend="USING_ARGTRAITS"> templatized</link> and will attempt to parse
the string its flag matches on the command line as the type it is
specified as. <classname>ValueArg&lt;int&gt;</classname>
will attempt to parse an
int, <classname>ValueArg&lt;float&gt;</classname> will attempt to
parse a float, etc. If <methodname>operator&gt;&gt;</methodname>
for the specified type doesn't
recognize the string on the command line as its defined type, that's
a command-line parsing error like any other -- reported via the
<classname>ParseOutcome</classname> that <methodname>parse()</methodname>
returns (see the note about <methodname>parseOrExit()</methodname>
<link linkend="EXAMPLE">above</link>), not an exception.
</para>
</sect1>

<sect1 id="MULTI_ARG">
<title><classname>MultiArg</classname></title>
<para>
A <classname>MultiArg</classname> is a <classname>ValueArg</classname> that
can be specified more than once on a command line and instead of returning
a single value, returns a <classname>vector</classname> of values.
</para>
<para>
Imagine a compiler that allows you to specify multiple directories
to search for libraries...
</para>

<programlisting>
                % fooCompiler -L /dir/num1 -L /dir/num2 file.foo
</programlisting>
<para>
This will be reported as a parse error if you try to do this
with a <classname>ValueArg</classname> or a <classname>SwitchArg</classname>.
In situations like this, you will want to use a
<classname>MultiArg</classname>. A
<classname>MultiArg</classname> is essentially a
<classname>ValueArg</classname> that appends any
value that it matches and parses onto a vector of values. When the
<methodname>value()</methodname> method is called, a vector of
values, instead of a single value is returned. A
<classname>MultiArg</classname> is declared much like
a <classname>ValueArg</classname>:


<programlisting>
                MultiArg&lt;int&gt; itest({.flag = "i", .name = "intTest",
                                    .description = "multi int test",
                                    .typeDesc = "int"});
                cmd.add( itest );
</programlisting>
Note that <classname>MultiArg</classname>s can be added to the
<classname>CmdLine</classname> in any order (unlike
<link linkend="UNLABELED_MULTI_ARG"> UnlabeledMultiArg</link>).
</para>
</sect1>

<sect1 id="MULTI_SWITCH_ARG">
<title><classname>MultiSwitchArg</classname></title>
<para>
A <classname>MultiSwitchArg</classname> is a <classname>SwitchArg</classname>
that can be specified more than once on a command line.
This can be useful
when command lines are constructed automatically from within other applications
or when a switch occurring
more than once indicates a value (-V means a little verbose -V -V -V means a lot
verbose), You can use a <classname>MultiSwitchArg</classname>.
The call
to <methodname>value()</methodname> for a <classname>MultiSwitchArg</classname> returns the number (int) of times
the switch has been found on the command line in addition to the default value.
Here is an example using the default initial value of 0:
<programlisting>
	MultiSwitchArg quiet({.flag = "q", .name = "quiet",
	                      .description = "Reduce the volume of output"});
	cmd.add( quiet );
</programlisting>
Alternatively, you can specify your own initial value:
<programlisting>
	MultiSwitchArg quiet({.flag = "q", .name = "quiet",
	                      .description = "Reduce the volume of output",
	                      .initialValue = 5});
	cmd.add( quiet );
</programlisting>
</para>
</sect1>

<sect1 id="UNLABELED_VALUE_ARG">
<title><classname>UnlabeledValueArg</classname></title>
<para>
An <classname>UnlabeledValueArg</classname> is a <classname>ValueArg</classname> that is not identified by a flag on the command line.  Instead
<classname>UnlabeledValueArg</classname>s are identified by their position in
the argv array.
</para>
<para>
To this point all of our arguments have had labels (flags)
identifying them on the command line, but there are some
situations where flags are burdensome and not worth the effort. One
example might be if you want to implement a magical command we'll
call <command>copy</command>. All <command>copy</command> does is
copy the file specified
in the first argument to the file specified in the second argument.
We can do this using <classname>UnlabeledValueArg</classname>s which are pretty
much just <classname>ValueArg</classname>s without the flag specified,
which tells
the <classname>CmdLine</classname> object to treat them accordingly.
The code would look like this:

<programlisting>

                UnlabeledValueArg&lt;float&gt;  nolabel({.name = "name",
                                                    .description = "unlabeled test",
                                                    .required = true,
                                                    .defaultValue = 3.14,
                                                    .typeDesc = "nameString"});
                cmd.add( nolabel );

</programlisting>

Everything else is handled identically to what is seen above. The
only difference to be aware of, and this is important: <emphasis>the order
that UnlabeledValueArgs are added to the <classname>CmdLine</classname>
is the order that they will be parsed!!!!</emphasis>
This is <emphasis>not</emphasis> the case for normal
<classname>SwitchArg</classname>s and <classname>ValueArg</classname>s.
What happens internally is the first argument that the
<classname>CmdLine</classname> doesn't recognize is assumed to be
the first <classname>UnlabeledValueArg</classname> and
parses it as such. Note that you are allowed to intersperse labeled
args (SwitchArgs and ValueArgs) in between
<classname>UnlabeledValueArgs</classname> (either on the command line
or in the declaration), but the <classname>UnlabeledValueArgs</classname>
will still be parsed in the order they are added. Just remember that order is
important for unlabeled arguments.
</para>
</sect1>

<sect1 id="UNLABELED_MULTI_ARG">
<title><classname>UnlabeledMultiArg</classname></title>
<para>
An <classname>UnlabeledMultiArg</classname> is an <classname>UnlabeledValueArg</classname> that allows more than one value to be specified.  Only one
<classname>UnlabeledMultiArg</classname> can be specified per command line.
The <classname>UnlabeledMultiArg</classname> simply reads the remaining
values from argv up until -- or the end of the array is reached.
</para>
<para>
Say you want a strange command
that searches each file specified for a given string (let's call it
<command>grep</command>), but you don't want to have to type in all of the file
names or write a script to do it for you. Say,

<programlisting>
                % grep pattern *.txt
</programlisting>

First remember that the <emphasis>*</emphasis> is handled by the shell and
expanded accordingly, so what the program <command>grep</command> sees is
really something like:

<programlisting>
                % grep pattern file1.txt file2.txt fileZ.txt
</programlisting>

To handle situations where multiple, unlabeled arguments are needed,
we provide the <classname>UnlabeledMultiArg</classname>.
<classname>UnlabeledMultiArg</classname>s
are declared much like everything else, but generally only a name
and a description are needed. By default, if an
<classname>UnlabeledMultiArg</classname>
is specified, then at least one is required to be present or it's a
parse error. The most important thing to remember is,
that like <classname>UnlabeledValueArg</classname>s: order matters!
In fact, <emphasis>an UnlabeledMultiArg must be the last argument added to the
CmdLine!</emphasis>. Here is what a declaration looks like:

<programlisting>

                //
                // UnlabeledMultiArg must be the LAST argument added!
                //
                UnlabeledMultiArg&lt;string&gt; multi({.name = "fileNames",
                                                 .description = "file names"});
                cmd.add( multi );
                cmd.parseOrExit(argc, argv);

                vector&lt;string&gt;  fileNames = multi.value();

</programlisting>

You must only ever specify one (1) <classname>UnlabeledMultiArg</classname>.
One <classname>UnlabeledMultiArg</classname> will read every unlabeled
Arg that wasn't already processed by a
<classname>UnlabeledValueArg</classname> into a
<classname>vector</classname> of type T. Any
<classname>UnlabeledValueArg</classname> or other
<classname>UnlabeledMultiArg</classname> specified after the first
<classname>UnlabeledMultiArg</classname> will be ignored, and if
they are required,
it's a parse error. When you call the
<methodname>value()</methodname>
method of the <classname>UnlabeledValueArg</classname> argument,
a <classname>vector</classname>
will be returned. If you can imagine a situation where there will
be multiple args of multiple types (stings, ints, floats, etc.)
then just declare the <classname>UnlabeledMultiArg</classname> as type
<classname>string</classname> and parse the different values yourself or use
several <classname>UnlabeledValueArg</classname>s.
</para>
</sect1>
</chapter>

<chapter id="COMPLICATIONS">
<title>Complications</title>
<para>
Naturally, what we have seen to this point doesn't satisfy all of
our needs.
</para>

<sect1 id="COMBINE_SWITCHES">
<title>I want to combine multiple switches into one argument...</title>
<para>
Multiple <classname>SwitchArg</classname>s can be combined into a
single argument on the command line. If you have switches -a, -b and -c
it is valid to do either:

<programlisting>
                % command -a -b -c
</programlisting>

<emphasis>or</emphasis>

<programlisting>
                % command -abc
</programlisting>

<emphasis>or</emphasis>

<programlisting>
                % command -ba -c
</programlisting>

This is to make this library more in line with the POSIX and GNU
standards (as I understand them).
</para>
</sect1>

<sect1 id="ARG_GROUP">
<title>I want one argument or the other, but not both...</title>
<para>
Suppose you have a command that must read input from one of two
possible locations, either a local file or a URL. The command
<emphasis>must</emphasis> read something, so <emphasis>one</emphasis>
argument is required, but not both, yet neither argument is strictly
necessary by itself.

TCLAP can automatically enforce such constraints on a set of arguments
using an argument group of type <classname>OneOf</classname>.
</para>

<para>
<programlisting>

        ValueArg&lt;string&gt;  fileArg({.flag = "f", .name = "file",
                                   .description = "File name to read",
                                   .defaultValue = "/dev/zero",
                                   .typeDesc = "filename"});
        ValueArg&lt;string&gt;  urlArg({.flag = "u", .name = "url",
                                  .description = "URL to load",
                                  .defaultValue = "http://example.com",
                                  .typeDesc = "URL"});

        OneOf input;
        input.add(fileArg).add(urlArg);
        cmd.add(input);
        cmd.parseOrExit(argc, argv);

</programlisting>

If the <classname>Arg</classname> has been matched on the command
line, the <methodname>isSet()</methodname> will return
<constant>TRUE</constant>.

<programlisting>

        if ( fileArg.isSet() )
                readFile( fileArg.value() );
        else if ( urlArg.isSet() )
                readURL( urlArg.value() );
        else
                // Should never get here because TCLAP will note that one of the
                // required args above has not been set.
                throw("Very bad things...");

</programlisting>

It is helpful to note that <classname>Arg</classname>s of any type can
be part of the same argument group This means that you can add a
<classname>SwitchArg</classname> and a
<classname>ValueArg</classname>. This is helpful in situations where
one of several options is necessary and one of the options requires
additional information.
</para>
</sect1>

<sect1 id="ARG_GROUP_EITHEROF">
<title>...but I don't want to require either one</title>
<para>
There is also the class <classname>EitherOf</classname> that works the
same way, except it does not require any argument to be set. This can
be useful if there is a natural default. For example, selecting the
encryption algorithm may default to AES, but there may be flags for
choosing another algorithm and they are all mutually exclusive.
</para>
</sect1>

<sect1 id="NO_FLAG">
<title>I have more arguments than single flags make sense for...</title>
<para>
Some commands have so many options that single flags no longer map
sensibly to the available options. In this case, it is desirable to
specify <classname>Arg</classname>s using only long options. This one is easy to
accomplish, just make the flag value blank in the <classname>Arg</classname>
constructor. This will tell the <classname>Arg</classname> that only the long
option should be matched and will force users to specify the long
option on the command line. The help output is updated accordingly.

<programlisting>

        ValueArg&lt;string&gt;  fileArg({.flag = "", .name = "file",
                                   .description = "File name",
                                   .required = true, .defaultValue = "homer",
                                   .typeDesc = "filename"});

        SwitchArg  caseSwitch({.flag = "", .name = "upperCase",
                               .description = "Print in upper case"});

</programlisting>
</para>
</sect1>


<sect1 id="CONSTRAINT">
<title>I want to constrain the values allowed for a particular
argument...</title>

<para>
For example, one might wish to allow only integers greater than 0. In
this case, simply create a class that implements the
<classname>Constraint&lt;int&gt;</classname> interface and checks
whether the value parsed is greater than 0 (done in the
<methodname>check()</methodname> method) and set the
<code>.constraint</code> field of your
<classname>Arg</classname>'s spec to point at your new
<classname>Constraint</classname>.
</para>

<para>
  <emphasis>TCLAP</emphasis> provides a simple way to constrain the
  allowed values to a list of values. Simply create a
  <classname>ValuesConstraint</classname> object with a
  <classname>vector</classname> of values and set it as the
  <classname>Arg</classname>'s <code>.constraint</code>.
</para>

<para>
When the value for the <classname>Arg</classname> is parsed, it is
checked against the list of values specified in the
<classname>ValuesConstraint</classname>.  If the value is in the list
then it is accepted. If not, it's a parse error. Here is a
simple example:

<programlisting>
		vector&lt;string&gt; allowed;
		allowed.push_back("homer");
		allowed.push_back("marge");
		allowed.push_back("bart");
		allowed.push_back("lisa");
		allowed.push_back("maggie");
		ValuesConstraint&lt;string&gt; allowedVals( allowed );

		ValueArg&lt;string&gt; nameArg({.flag = "n", .name = "name",
		                          .description = "Name to print",
		                          .required = true, .defaultValue = "homer",
		                          .constraint = &amp;allowedVals});
		cmd.add( nameArg );
</programlisting>

When a <classname>ValuesConstraint</classname> is specified,
instead of a type description being specified in the
<classname>Arg</classname>, a
type description is created by concatenating the values in the
allowed list using operator&lt;&lt; for the specified type. The
help/usage for the <classname>Arg</classname> therefore lists the
allowable values.  Because of this, you might want to keep the list
relatively small, however there is no limit on this.
</para>
</sect1>


<sect1 id="ARG_ADD_CMDLINE">
<title>Why do I have to call cmd.add() myself now?</title>
<para>
Prior to TCLAP 2.0, every <classname>Arg</classname> type had an extra
constructor overload that took a <classname>CmdLine</classname> object
as a parameter and added itself to it automatically as part of
construction, so a separate <methodname>add()</methodname> call
wasn't necessary:

<programlisting>

        // Pre-2.0 only -- this constructor overload no longer exists.
        SwitchArg btest("B", "existTestB", "exist Test B", cmd, false);

</programlisting>

TCLAP 2.0 removed these self-registering constructors: every
<classname>Arg</classname> subtype now has exactly one constructor,
taking the single spec aggregate described <link
linkend="ARG_PROPERTIES">above</link>. Always construct the
<classname>Arg</classname> and then call <methodname>add()</methodname>
explicitly, as two separate statements:

<programlisting>

        // Create the command line.
        CmdLine cmd({.message = "this is a message",
                     .dialect = {.delimiter = '='},
                     .version = "0.99"});

        // Construct each Arg, then add() it -- there is no longer a
        // constructor overload that does both at once.
        SwitchArg btest({.flag = "B", .name = "existTestB",
                         .description = "exist Test B"});
        cmd.add(btest);

        ValueArg&lt;string&gt; stest({.flag = "s", .name = "stringTest",
                                .description = "string test",
                                .required = true,
                                .defaultValue = "homer"});
        cmd.add(stest);

        UnlabeledValueArg&lt;string&gt; utest({.name = "unTest1",
                                         .description = "unlabeled test one",
                                         .defaultValue = "default",
                                         .typeDesc = "string"});
        cmd.add(utest);

        // Parse the command line.
        cmd.parseOrExit(argc, argv);

</programlisting>

If you're migrating code from 1.4/1.6, this is one of the two
mechanical changes every construction site needs (the other is
switching from positional constructor parameters to the named fields
described <link linkend="ARG_PROPERTIES">above</link>): find every
<methodname>Arg(...)</methodname> call that passed a
<parameter>cmd</parameter> object as a constructor argument, and split
it into a construction statement followed by a separate
<methodname>cmd.add(...)</methodname> call.
</para>

<para>
If the only reason you want the <classname>Arg</classname> constructed
and added in one step is convenience, not an independently-scoped
local variable, <classname>CmdLine</classname> offers a one-statement
alternative: <methodname>addOwned&lt;ArgType&gt;()</methodname>. It
constructs the <classname>Arg</classname>, has the
<classname>CmdLine</classname> heap-own it (destroyed alongside the
<classname>CmdLine</classname>), registers it, and returns a reference
bound to the <classname>CmdLine</classname>'s lifetime rather than the
caller's scope:

<programlisting>

        // ArgType must be given explicitly -- it can't be deduced from
        // the spec alone.
        SwitchArg &amp;btest = cmd.addOwned&lt;SwitchArg&gt;(
            {.flag = "B", .name = "existTestB", .description = "exist Test B"});

</programlisting>

This is most useful when you're building an unknown number of
<classname>Arg</classname>s at runtime (e.g. in a loop) and don't need
each one to be an independently-scoped local variable. For the common
case of a fixed, known set of arguments, the two-statement
<methodname>add()</methodname> form above remains the default most
code uses.
</para>
</sect1>

<sect1 id="CHANGE_OUTPUT">
<title>I want different output than what is provided...</title>
<para>
It is straightforward to change the output generated by
<emphasis>TCLAP</emphasis>.  Either subclass the
<classname>StdOutput</classname> class and re-implement the methods you choose,
or write your own class that implements the
<classname>CmdLineOutput</classname> interface.  Once you have done this,
then use the <classname>CmdLine</classname> <methodname>setOutput</methodname>
method to tell the <classname>CmdLine</classname> to use your new output
class. Here is a simple example:
<programlisting>
class MyOutput : public StdOutput
{
	public:
		virtual void failure(CmdLineInterface&amp; c, ArgException&amp; e)
		{
			cerr &lt;&lt; "My special failure message for: " &lt;&lt; endl
				 &lt;&lt; e.what() &lt;&lt; endl;
			exit(1);
		}

		virtual void usage(CmdLineInterface&amp; c)
		{
			cout &lt;&lt; "my usage message:" &lt;&lt; endl;
			list&lt;Arg*&gt; args = c.getArgList();
			for (ArgListIterator it = args.begin(); it != args.end(); it++)
				cout &lt;&lt; (*it)-&gt;longID()
					 &lt;&lt; "  (" &lt;&lt; (*it)-&gt;getDescription() &lt;&lt; ")" &lt;&lt; endl;
		}

		virtual void version(CmdLineInterface&amp; c)
		{
			cout &lt;&lt; "my version message: 0.1" &lt;&lt; endl;
		}
};

int main(int argc, char** argv)
{
		CmdLine cmd({.message = "this is a message", .version = "0.99"});

		// set the output
		MyOutput my;
		cmd.setOutput( &amp;my );

		// proceed normally ...
</programlisting>

See <filename>test4.cpp</filename> in the examples directory for the full
example. <emphasis>NOTE</emphasis>: if you supply your own Output object, we
will not delete it in the <classname>CmdLine</classname> destructor.  This
could lead to a (very small) memory leak if you don't take care of the object
yourself. Also note that in 2.0, <methodname>failure</methodname> --
whether it's <classname>StdOutput</classname>'s default implementation or
your own override -- purely reports the error; it no longer exits the
application itself. Exiting is <methodname>parseOrExit()</methodname>'s
job (see <link linkend="EXAMPLE">above</link>), based on the
<classname>ParseOutcome</classname> that <methodname>parse()</methodname>
returns, not something <methodname>failure</methodname> triggers as a
side effect. If you override <methodname>failure</methodname> and still
want it to terminate the program directly -- as <filename>test4.cpp</filename>
does, to preserve its original point of demonstrating a fully custom error
path -- call <function>exit()</function> yourself at the end of your
override, the same way the example does.
</para>
</sect1>

<sect1 id="NO_HELP_VERSION">
<title>I don't want the --help and --version switches to be created automatically...</title>
<para>
Help and version information is useful for nearly all command line applications
and as such we generate flags that provide those options automatically.
However, there are situations when these flags are undesirable. For these
cases the <classname>CmdLine</classname> spec has a
<code>.helpAndVersion</code> field. Setting it to
<parameter>false</parameter> will disable automatic help and version
generation.
<programlisting>
		CmdLine cmd({.message = "this is a message", .version = "0.99",
		             .helpAndVersion = false});
</programlisting>
</para>
</sect1>

<sect1 id="IGNORE_ARGS">
<title>I want to ignore certain arguments...</title>
<para>
The <parameter>--</parameter> flag is automatically included in the
<classname>CmdLine</classname>.
As (almost) per POSIX and GNU standards, any argument specified
after the <parameter>--</parameter> flag is ignored.
<emphasis>Almost</emphasis> because if an
<classname>UnlabeledValueArg</classname> that has not been set or an
<classname>UnlabeledMultiArg</classname> has been specified, by default
we will assign any arguments beyond the <parameter>--</parameter>
to the those arguments as
per the rules above. This is primarily useful if you want to pass
in arguments with a dash as the first character of the argument. It
should be noted that even if the <parameter>--</parameter> flag is
passed on the command line, the <classname>CmdLine</classname> will
<emphasis>still</emphasis> test to make sure all of the required
arguments are present.
</para>

<para>
Of course, this isn't how POSIX/GNU handle things, they explicitly
ignore arguments after the <parameter>--</parameter>. To accommodate this,
we can make both <classname>UnlabeledValueArg</classname>s and
<classname>UnlabeledMultiArg</classname>s ignoreable by setting their
spec's <code>.ignoreable</code> field to <parameter>true</parameter>.
See the <ulink url="html/index.html"> API Documentation</ulink> for details.
</para>
</sect1>

<sect1 id="IGNORE_UNMATCHED">
<title>I want to ignore unmatched arguments...</title>
<para>
By default, if <emphasis>TCLAP</emphasis> sees an argument that doesn't
match a specified <classname>Arg</classname>, it is a parse error.
This strict handling provides some assurance that all input to a program
is controlled. However, there are times when
this strict handling of arguments might not be desirable.
<emphasis>TCLAP</emphasis> provides two alternatives. The first is to
add an <classname>UnlabeledMultiArg</classname> to the command line. If
this is done, all unmatched arguments will get added to this arg.

The second option is that
that <emphasis>TCLAP</emphasis> can simply ignore any unmatched
arguments on the command line. This is accomplished by calling the
<methodname>ignoreUnmatched</methodname> method with
<parameter>true</parameter> on the
<classname>CmdLine</classname> object that's been constructed.

<programlisting>

    // Define the command line object.
	CmdLine cmd({.message = "Command description message", .version = "0.9"});

	// Tell the command line to ignore any unmatched args.
	cmd.ignoreUnmatched(true);

    // Define a value argument and add it to the command line.
	ValueArg&lt;string&gt; nameArg({.flag = "n", .name = "name",
	                          .description = "Name to print",
	                          .required = true, .defaultValue = "homer"});
	cmd.add( nameArg );

    // Parse the args.
    cmd.parseOrExit(argc, argv);

</programlisting>

Given the program above, if a user were to type:

<programlisting>

	% command -n Mike something to ignore

</programlisting>

The program would succeed and the name <classname>ValueArg</classname>
would be populated with "Mike" but
the strings "something", "to", and "ignore" would simply be ignored by the
parser.
</para>
<para>
<emphasis>NOTE:</emphasis> If both <methodname>ignoreUnmatched</methodname>
is set to true and an <classname>UnlabeledMultiArg</classname> is added to
the command line, then the <classname>UnlabeledMultiArg</classname> will
"win" and all extra arguments will be added to it rather than be ignored.
</para>
</sect1>

<sect1 id="READING_HEX_INTEGERS">
<title>I want to read hex integers as arguments...</title>
<para>
Sometimes it's desirable to read integers formatted in decimal,
hexadecimal, and octal format. Wrap the value type in
<classname>AutoBaseInt&lt;T&gt;</classname> (instead of a bare integral
type) to opt a single <classname>ValueArg</classname> or
<classname>MultiArg</classname> into C-style auto-base parsing: a
<parameter>0x</parameter>/<parameter>0X</parameter> prefix selects
base 16, a leading <parameter>0</parameter> followed by another digit
selects base 8, and otherwise base 10 is used.
<programlisting>

#include "tclap/CmdLine.h"
#include &lt;iostream&gt;

using namespace TCLAP;
using namespace std;

int main(int argc, char** argv)
{

	CmdLine cmd({.message = "this is a message", .version = "0.99"});

	ValueArg&lt;AutoBaseInt&lt;int&gt;&gt; itest({.flag = "i", .name = "intTest",
	                                  .description = "integer test",
	                                  .required = true, .defaultValue = 5});
	cmd.add( itest );

	//
	// Parse the command line.
	//
	cmd.parseOrExit(argc, argv);

	//
	// Set variables
	//
	int _intTest = itest.value();
	cout &lt;&lt; "found int: " &lt;&lt; _intTest &lt;&lt; endl;
}
</programlisting>
</para>
<para>
Unlike the pre-2.0 <parameter>TCLAP_SETBASE_ZERO</parameter> compile-time
macro this replaces (which applied to every integer
<classname>ValueArg</classname>/<classname>MultiArg</classname> in the
whole program at once, whether you wanted it there or not),
<classname>AutoBaseInt&lt;T&gt;</classname> is an explicit, per-Arg
opt-in: auto-base and decimal-only integer Args can coexist in the same
program. <classname>AutoBaseInt&lt;T&gt;</classname> converts implicitly
to <parameter>T</parameter>, so <parameter>_intTest</parameter> above
could just as well have been declared
<parameter>AutoBaseInt&lt;int&gt;</parameter> directly instead of
<parameter>int</parameter>.
</para>
</sect1>

<sect1 id="USING_ARGTRAITS">
<title>I want to use different types...</title>
<para>
The usual C++ types (int, long, bool, etc.) are supported by <emphasis>TCLAP</emphasis> out
of the box. As
long as operator&gt;&gt; and operator&lt;&lt; are supported, other types should work fine
too, you'll just need to specify the <classname>ArgTraits</classname> which
tells <emphasis>TCLAP</emphasis> how you expect the type to be handled.
</para>

<para>
For example, assume that you'd like to read one argument on the
command line in as a <classname>std::pair</classname> object. You need
to tell <emphasis>TCLAP</emphasis> whether to treat
<classname>std::pair</classname> as a String or Value (default).
StringLike means to treat the string on the command line as a string
and use it directly (by assignment operator), whereas ValueLike means
that a value object should be extracted from the string using
operator&gt;&gt;. For <classname>std::pair</classname> we'll choose
ValueLike. Here is an example of how to do this for int,double pair
(see <filename>test47.cpp</filename>):

<programlisting>
#include &lt;iostream&gt;
#include &lt;utility&gt;

// We need to tell TCLAP how to parse our pair, we assume it will be
// given as two arguments separated by whitespace.
std::istream &amp;operator&gt;&gt;(std::istream &amp;is, std::pair&lt;int, double&gt; &amp;p) {
  return is &gt;&gt; p.first &gt;&gt; p.second;
}

// Make it easy to print values of our type.
std::ostream &amp;operator&lt;&lt;(std::ostream &amp;os, const std::pair&lt;int, double&gt; &amp;p) {
  return os &lt;&lt; p.first &lt;&lt; ' ' &lt;&lt; p.second;
}

#include "tclap/CmdLine.h"
using namespace TCLAP;

// Our pair can now be used as any other type.
int main(int argc, char **argv) {
  CmdLine cmd({.message = "test pair argument"});
  ValueArg&lt;std::pair&lt;int, double&gt; &gt; parg({.flag = "p", .name = "pair",
                                         .description = "int,double pair",
                                         .required = true,
                                         .defaultValue = std::make_pair(0, 0.0),
                                         .typeDesc = "int,double"});
  cmd.add(parg);
  cmd.parseOrExit(argc, argv);
  std::cout &lt;&lt; parg.value() &lt;&lt; std::endl;
}

</programlisting>

<emphasis>Important:</emphasis> When specifying the argument on the command line it
needs to be given as a single argument, that is by quoting it. For
example, <code>./test47 -p "1 2.3"</code>.
</para>

<para>
If you do not want to provide operator&gt;&gt;, you can instead use
operator= by defining the type as
<classname>StringLike</classname>. A complete example is given in
<filename>test11.cpp</filename>, but the main difference is you need
to tell TCLAP to assign to the type using operator=. This is done by
defining it as <classname>StringLike</classname> as shown below:

<programlisting>
namespace TCLAP {
template&lt;&gt;
struct ArgTraits&lt;Vect3D&gt; {
    typedef StringLike ValueCategory;
};
}
</programlisting>
</para>

</sect1>

<sect1 id="CHANGING_STARTSTRINGS">
<title>I want to use Windows-style flags like "/x" and "/y"...</title>
<para>
It is traditional in Posix environments that the "-" and "--" strings are used to signify
the beginning of argument flags and long argument names. However, other environments,
namely Windows, use different strings.  <emphasis>TCLAP</emphasis> lets you
control which strings are used per <classname>CmdLine</classname>, by
setting the <code>.dialect</code> field of its spec to a
<classname>Dialect</classname> with the prefixes you want. This is an
ordinary runtime value now, not a set of compile-time
<methodname>#define</methodname> directives that applied to the whole
program at once -- two different <classname>CmdLine</classname>s in
the same program can even use different conventions. Here is an
example:

<programlisting>
//
// This illustrates how to change the flag and name start strings for
// Windows, otherwise the defaults are used.
//
#include "tclap/CmdLine.h"

using namespace TCLAP;
using namespace std;

int main(int argc, char** argv)
{
	CmdLine cmd({.message = "Command description message",
	             .dialect = Dialect{.flagPrefix = "/",
	                                .namePrefix = "~~"},
	             .version = "0.9"});

	// Everything else is identical!
	...
</programlisting>

<classname>Dialect</classname> also has a <code>.delimiter</code> field
(default <parameter>' '</parameter>) for the character that separates
a flag/name from its value -- see the delimiter note in
<link linkend="LIBRARY_PROPERTIES">Library Properties</link>, above.

</para>
</sect1>

</chapter>

<chapter id="MORE_INFO">
<title>More Information</title>
<para>
For more information, look at the <ulink url="html/index.html">
API Documentation</ulink> and the examples included with the
distribution.
</para>

<para>
<emphasis>Happy coding!</emphasis>
</para>

</chapter>
</book>
